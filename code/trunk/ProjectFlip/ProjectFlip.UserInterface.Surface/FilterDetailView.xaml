<UserControl x:Class="ProjectFlip.UserInterface.Surface.FilterDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="http://schemas.microsoft.com/surface/2008">

    <UserControl.Resources>
        <DataTemplate x:Key="FilterCriteriasTemplate">
            <s:SurfaceButton Background="Yellow" >
            </s:SurfaceButton>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="#AA000000">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="600"/>
        </Grid.ColumnDefinitions>
        
        <s:SurfaceListBox Background="Gray" VerticalAlignment="Center" Grid.Column="0" ItemsSource="{Binding Criterias}" ItemTemplate="{DynamicResource FilterCriteriasTemplate}">
        </s:SurfaceListBox>
        
        <s:SurfaceListBox Background="Gray" Height="400" VerticalAlignment="Center" Grid.Column="1">
            <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding CurrentProjectNote.Focus}"  ></ListView>
        </s:SurfaceListBox>
        
    </Grid>
</UserControl>
