<UserControl x:Class="ProjectFlip.UserInterface.Surface.FilterDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="http://schemas.microsoft.com/surface/2008">

    <UserControl.Resources>
        <DataTemplate x:Key="FilterMainCriteriaTemplate">
            <s:SurfaceButton Background="Yellow" >
                <TextBlock Text="{Binding Name}"></TextBlock>
            </s:SurfaceButton>
        </DataTemplate>

        <DataTemplate x:Key="FilterSubCriteriaTemplate">
            <s:SurfaceButton CommandParameter="{Binding}"  Command="{Binding DataContext.AddFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type s:SurfaceListBox}}}" Background="Orange" >
                <TextBlock Text="{Binding Description}"></TextBlock>
            </s:SurfaceButton>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="#AA000000">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="600"/>
        </Grid.ColumnDefinitions>

        <s:SurfaceListBox Background="Gray" VerticalAlignment="Center" Grid.Column="0" ItemsSource="{Binding Maincriteria}" ItemTemplate="{DynamicResource FilterMainCriteriaTemplate}">
        </s:SurfaceListBox>
        
        <s:SurfaceListBox Background="Gray" Height="400" VerticalAlignment="Center" Grid.Column="1" ItemsSource="{Binding Subcriteria}" ItemTemplate="{DynamicResource FilterSubCriteriaTemplate}" >
        </s:SurfaceListBox>
        
    </Grid>
</UserControl>
