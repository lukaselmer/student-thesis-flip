<UserControl x:Class="ProjectFlip.UserInterface.Surface.FilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="clr-namespace:Microsoft.Surface.Presentation.Controls;assembly=Microsoft.Surface.Presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">

    <UserControl.Resources>
    <DataTemplate x:Key="FilterCriteriaTemplate">
        <s:SurfaceButton CommandParameter="{Binding}"  Command="{Binding DataContext.RemoveFilterCommand, RelativeSource={RelativeSource AncestorType={x:Type s:SurfaceListBox}}}" Background="Orange" >
            <TextBlock Text="{Binding Description}"></TextBlock>
        </s:SurfaceButton>
    </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <s:SurfaceListBox ItemsSource="{Binding Filters}" ItemTemplate="{DynamicResource FilterCriteriaTemplate}">
        </s:SurfaceListBox> 
        <s:SurfaceButton Background="Orange" Width="150" Height="70" Command="{Binding ShowFilterCommand}" >
            <TextBlock Text="Kriterium hinzufügen" TextWrapping="WrapWithOverflow" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock></s:SurfaceButton>
    </Grid>
</UserControl>
